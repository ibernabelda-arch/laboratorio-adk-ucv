[project]
name = "laboratorio-adk-ucv"
version = "0.1.0"
description = ""
authors = [
    {name = "Ivan Bernabel", email = "ibernabelda@ucvvirtual.edu.pe"}
]
requires-python = ">=3.11,<4.0"
dependencies = [
    "google-adk (>=2.1.0,<3.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.3,<10.0.0)"
]

[tool.poetry]
package-mode = false